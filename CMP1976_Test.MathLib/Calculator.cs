

namespace CMP1976_Test.MathLib;

public class Calculator
{
    public decimal Add(decimal left, decimal right)
    {
        return left + right;
    }

    public decimal Divide(decimal dividend, decimal divisor)
    {
        return dividend/divisor;
    }
}
